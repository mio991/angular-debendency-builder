{
  "name": "angular-dependency-builder",
  "version": "0.1.0",
  "description": "Build Angular Dependencies",
  "files": [
    "dist",
    "src/schema.json",
    "builders.json"
  ],
  "scripts": {
    "prepack": "npm run build",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "builders": "builders.json",
  "author": "mio991",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "@angular-devkit/architect": "^0.1100.4",
    "@angular-devkit/build-angular": "^0.1100.4",
    "@angular-devkit/core": "^11.0.4",
    "rxjs": "^6.6.3"
  }
}